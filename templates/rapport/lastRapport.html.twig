{% extends 'base.html.twig' %}

{% block title %}Last version{% endblock %}

{% block body %}
    <div class="mt-3">
        {{form_start(lastForm)}}
        <h1 >Version finale </h1>
        {{form_widget(lastForm.last_version)}}
        <input type="submit" name="submit" value="Valider"class="btn btn-success col-4 center mt-2 {% if app.user.rapport.firstVersionEstDeploye == false  %} disabled {% endif %}">                                                     
    {{form_end(lastForm)}}
    </div>
{% endblock %}
  