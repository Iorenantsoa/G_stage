{% extends 'base.html.twig' %}

{% block title %}First version{% endblock %}

{% block body %}
    <div class="mt-3">
        {{form_start(firstForm)}}
        <h1 >Première Version</h1>
        {{form_widget(firstForm.first_version)}}
        <input type="submit" name="submit" value="Valider"class="btn btn-success col-4 center mt-2  " >
    {{form_end(firstForm)}}
    </div>
    
{% endblock %}