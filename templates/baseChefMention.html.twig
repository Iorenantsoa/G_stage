<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="{{asset('node_modules/font-awesome/css/font-awesome.min.css')}}">
  <link rel="stylesheet" href="{{asset('assets/admin/bootstrap/css/bootstrap.min.css')}}">
    <link href="{{asset('assets/admin/dashboard.css')}}" rel="stylesheet">
  <title>{% block title %}Welcome!{% endblock %}</title>
</head>
<body>
 
  <header class="navbar navbar-dark sticky-top bg-nav flex-md-nowrap p-0 shadow">
    <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3 fs-6" href="#">G-stage</a>
    <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- <input class="form-control form-control-dark w-100 rounded-0 border-0" type="text" placeholder="Search" aria-label="Search"> -->
    <div class="form-control form-control-dark w-100 rounded-0 border-0"></div>
    <div class="navbar-nav">
      <div class="nav-item text-nowrap">
          <a href="{{path('app_profile_chef_mention')}}" style="color:white;text-decoration:none; margin-right:20px;"><img src={{ asset('uploads/picture_profile/' ~ app.user.image) }}  alt="" style="margin-right:6px;width:30px ; height:30px;border-radius:50%;object-fit:cover;"> <span >{{app.user.name}} {{app.user.firstname}}</span></a>
      </div>
    </div>
    <div class="navbar-nav">
      <div class="nav-item text-nowrap">
        <a class="nav-link px-4" href="" data-bs-toggle="modal" data-bs-target="#logout" style="color:white;text-decoration:none;"><i class="fa fa-trash"></i> Déconnexion</a>
      </div>
      
    </div>
    
  </header>
 
  <div class="container-fluid">
    <div class="row">
      <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
        <div class="position-sticky pt-3 sidebar-sticky">
          <ul class="nav flex-column">
            <li class="nav-item">
              <a class="nav-link " aria-current="page" href="{{path('app_dashboard')}}">
                <span data-feather="home" class="align-text-bottom"></span>
                Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link " aria-current="page" href="{{path('app_all_user_by_admin')}}">
                <span data-feather="home" class="align-text-bottom"></span>
                Tous les stages
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path("app_etudiant_encadreur")}}">
                <span data-feather="file" class="align-text-bottom"></span>
                La liste des étudiants par enseignant
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('app_etudiant_sans_encadreur')}}">
                <span data-feather="file" class="align-text-bottom"></span>
                La liste des étudiants sans encadrants pédagogiques
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link " href="{{path('app_etudiant_sans_rapprort')}}">
                <span data-feather="shopping-cart" class="align-text-bottom"></span>
                La liste des étudiants qui n'ont pas déposé le rapport
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link"  href="{{path('app_liste_stage_valide')}}">
             
                <span data-feather="users" class="align-text-bottom"></span>
                La liste des stages validés pour la soutenance
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link"  href="{{path('app_liste_note_etudiant')}}">
             
                <span data-feather="users" class="align-text-bottom"></span>
                Les notes attribués aux étudiants
              </a>
            </li>
          </ul>
  
          
        </div>
      </nav>
    
      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <div class="container ">
        {% for message in app.session.flashBag.get('info') %}
            <div class="alert alert-info text-center" role="alert">
              {{message}}
            </div>  
        {% endfor %}
        {% for message in app.session.flashBag.get('success') %}
            <div class="alert alert-success text-center" role="alert">
              {{message}}
            </div>  
        {% endfor %}
        {% for message in app.session.flashBag.get('danger') %}
            <div class="alert alert-danger text-center" role="alert">
              {{message}}
            </div>  
        {% endfor %}
        
        
      
      {% block body %}

      {% endblock %}

     
      {% block logout %}
          {% include "fragments/_logout_fragment.html.twig" %}
      {% endblock %}
      
    </div>
      
      </main>
    </div>
  </div>
  
      <script src="{{asset('node_modules/jquery/dist/jquery.min.js')}}" type="text/javascript"></script>
      <script src="{{asset('assets/admin/bootstrap/js/bootstrap.bundle.min.js')}}"></script>
      <script type="text/javascript">
            {% block javascript %}

            {% endblock %}  
      </script>   
      </body>
</html>


